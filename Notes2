AVL/ Height Balanced Tree –
AVL tree is binary search tree with additional property that difference 
between height of left sub-tree and right sub-tree of any node can’t be more than 1


Mutex vs semaphore
Mutex is locking mechanism (one thread at a time)
Semaphore is signalling mechanism(Many threads at a time)

HashSet uses the implementation of the HashMap

It is mandatory to override the hashcode method if equals method is overriden

Conversions
Integer.toBinaryString();
Integer.toHexString();
Integer.toOctalString();

Integer.parseInt(str, 2);
Integer.parseInt(str, 8);
Integer.parseInt(str, 16);

Fibonacci Number - one of 5*n^2+4 or 5*n^2-4 must be a perfect square 
